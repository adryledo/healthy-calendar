import { StyleSheet } from 'react-native';
import { colors } from '../colors';

export const healthyCalendarScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.black,
    padding: 16,
  },
});
